Password Manager that creates a secure password.

It creates a Folder on the desktop, inside the folder "AUTOPLAY 2" it asks you to put an input that creates a {someName}.txt, which will contain your email, an username (if required) and a secure password.

When program finishes, it opens {someName}.txt.

The password will automatically be copied to your clipboard.
